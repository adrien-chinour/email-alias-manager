<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('email_index') }}">{{ 'site.title' | trans }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('email_index') }}">{{ 'My aliases' | trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('sync_diff') }}">{{ 'Synchronisation' | trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('export_select') }}">{{ 'Exporting aliases' | trans }}</a>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0" action="{{ path('search_alias') }}" method="get">
            <input class="form-control mr-sm-2" type="search" name="alias" required
                   placeholder="Rechercher un alias" aria-label="{{ 'Search alias' | trans }}">
            <button class="btn btn-outline-primary my-2 my-sm-0" type="submit">{{ 'Search' | trans }}</button>
        </form>
        <a class="btn btn-danger ml-2" href="{{ path('app_logout') }}">
            <i class="fas fa-sign-out-alt"></i>
        </a>
    </div>
</nav>
